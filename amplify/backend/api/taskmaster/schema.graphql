type Task @model {
  id: ID!
  title: String
  body: String
  state: Int
  description: String
  belongsTo: Team @connection(name: "taskTeams")
}

type Team @model {
  id: ID!
  name: String
  tasks: [Task] @connection(name: "taskTeams")
}
